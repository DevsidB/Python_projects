letter = "My Name is    Siddhesh and I am learning a  python course from code with    harry"
print (letter)
letter = letter.replace ("  "," ") # replace only doublespaces with single space...4 spaces are converted into two
letter = letter.replace ("  "," ") #'''again use the same function to replace the remaining str not replaced in the above syntax'''
print (letter)